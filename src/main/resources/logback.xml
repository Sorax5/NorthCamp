<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />

    <shutdownHook class="ch.qos.logback.core.hook.DefaultShutdownHook">
        <delay>2000</delay>
    </shutdownHook>

    <!-- ======================== -->
    <!--     Console Appender     -->
    <!-- ======================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan([%logger{20}]) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================== -->
    <!--   Fichier principal      -->
    <!-- ======================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>run/logs/northcamp.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>run/logs/northcamp.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================== -->
    <!--   Fichier erreurs only   -->
    <!-- ======================== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>run/logs/northcamp-errors.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>run/logs/northcamp-errors.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] - %msg%n%xException</pattern>
        </encoder>
    </appender>

    <!-- ======================== -->
    <!--   Async wrappers (perf)  -->
    <!-- ======================== -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="FILE" />
    </appender>

    <appender name="ASYNC_ERROR_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>256</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="ERROR_FILE" />
    </appender>

    <!-- ======================== -->
    <!--        Loggers           -->
    <!-- ======================== -->
    <logger name="fr.phylisiumstudio" level="DEBUG" />

    <logger name="net.minestom" level="INFO" />
    <logger name="com.google.inject" level="WARN" />
    <logger name="com.fasterxml.jackson" level="WARN" />
    <logger name="com.badlogic.gdx" level="WARN" />
    <logger name="org.spongepowered" level="WARN" />

    <!-- ======================== -->
    <!--          Root            -->
    <!-- ======================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
        <appender-ref ref="ASYNC_ERROR_FILE" />
    </root>

</configuration>

